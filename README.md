# Student-Management-API-
Student Management API is a simple REST API built with FastAPI that allows users to manage student records. It provides endpoints to view all students, retrieve a student by ID, and add new students. The project uses Pydantic for request validation and stores data in an in-memory Python list.
